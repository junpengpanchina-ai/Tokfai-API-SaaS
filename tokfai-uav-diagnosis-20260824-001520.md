# Tokfai UAV Diagnosis

- job_id: av_1787501720748_ru0u3mtn
- question: 详细解读这个飞控文件，重点看控制分配、姿态环、油门/电机输出和安全边界
- source_path: /tmp/uav-test.txt
- file_count: 1
- uploaded_count: 1
- analyze_status: analyzed

## 诊断结论
该文件属于 `FlightControl/ControlLaw/txg_control_task.c`，从命名和说明看，核心职责是飞控任务调度、姿态控制、control allocation、油门/电机输出以及安全边界处理。它应处在飞控主链路的中枢位置，负责把姿态/轨迹控制需求转换为可执行的执行机构输出，并在异常时触发降级或保护。

## 控制链路判断
- 输入侧：来自上层飞控状态、姿态指令、控制模式或任务调度事件。
- 控制侧：姿态环/相关控制律计算，产生期望力矩或控制量。
- 分配侧：control allocation 将控制量映射到油门/电机/舵面输出。
- 输出侧：执行机构输出更新，并受限于安全边界。

## 姿态环判断
- 文件很可能承载姿态闭环的关键任务调度点，至少负责周期性执行姿态控制律。
- 关注点应包括：环路周期一致性、积分饱和、限幅、指令平滑、输出抑制与模式切换时的状态重置。
- 若姿态环和分配逻辑耦合过紧，容易出现控制量与实际输出不一致的问题，需要检查控制律输出到 allocation 之间的约束是否完整。

## 任务调度判断
- `control_task` 命名表明这是周期任务或事件任务入口，而不是单纯算法库函数。
- 需要重点核查：调度频率、任务优先级、超时处理、重复触发保护、任务间共享状态的并发安全。
- 如果该任务同时承担计算和输出，必须确认在负载升高时不会导致控制周期漂移。

## 异常保护判断
- 已明确涉及“安全边界”，说明该文件应包含或调用保护逻辑。
- 典型风险点：姿态异常、传感器失效、控制量越界、输出饱和、模式非法切换、空值或未初始化状态。
- 合理的实现应具备：限幅、失效降级、输出冻结/归零、保护态锁存和恢复条件。

## 可交付诊断
- 这是一个飞控链路核心任务文件，优先级高，任何姿态环或分配问题都会直接影响飞行安全。
- 当前从抽取内容只能确认职责边界，不能确认具体 bug、参数错误或逻辑缺陷，因为缺少函数体与调用链。
- 下一步可交付的审查重点应是：任务入口、姿态控制律、allocation 映射、输出限幅、故障状态机、调度时序和保护分支。

TOKFAI_P1285_SERVER_FILE_INTAKE_DONE
