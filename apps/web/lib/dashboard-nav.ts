/** @deprecated Import from `@/lib/dashboard-safe/nav` — kept for compatibility. */
export {
  DASHBOARD_NAV_ITEMS,
  DASHBOARD_NAV_SECTIONS,
  isDashboardNavActive,
  type DashboardNavItem,
  type DashboardNavSection,
} from "@/lib/dashboard-safe/nav";
